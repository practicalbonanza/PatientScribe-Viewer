node_modules/

test-results/
playwright-report/
blob-report/
playwright/.cache/

.DS_Store

.env
.env.*
*.pem

# The deploy overlay. Parameter names are public and live in the template;
# their values are not — with one exception, ApiOrigin, which is supplied from
# here and is public by design — and this is where they land. Copy
# infra/parameters.json.example, fill it in, and it stays here.
infra/parameters.json

release-build/
